# Digital Business Card

A compact static configurable digital business.

## Configuration

The contents can be edited from the json files in src/resources/data/

## Running

### Requirements
- Node.js
- React

### Running locally
- clone the repository
- run `npm install`
- run `npm run dev`